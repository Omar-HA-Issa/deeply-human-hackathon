# Frontend (React + Vite)

This folder now contains a minimal React web app with an interactive globe
using `react-globe.gl`.

## Quick start

```bash
cd frontend
npm install
npm run dev
```

## Build

```bash
npm run build
npm run preview
```
